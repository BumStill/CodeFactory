# Increase rustc thread stack to 32 MB.
# Needed on Windows MSVC: icu_normalizer_data / icu_properties_data embed
# large Unicode tables and exhaust the default 8 MB stack.
[env]
RUST_MIN_STACK = "33554432"
