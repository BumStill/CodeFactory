# Unrelated scenario-gate probe

This temporary pull request verifies that a documentation-only change produces
an empty affected-scenario plan and does not start platform execution runners.
The probe is closed without merging after GitHub records the gate outcomes.
